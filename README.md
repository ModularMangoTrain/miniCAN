# miniCAN
CAN bus is widely used in motorsport and aerospace but requires dedicated hardware or microcontrollers with built-in CAN modules, so I made a minimal software driver protocol that mimics CAN, meant for small MCU.  Resources  Readme
